export default function Progress({ title, value }) {
  return (
    <div className="mb-4">
      <p className="text-sm mb-1">{title}</p>
      <div className="w-full bg-gray-200 h-2 rounded-full">
        <div
          className="bg-green-700 h-2 rounded-full"
          style={{ width: `${value}%` }}
        />
      </div>
    </div>
  );
}